name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - name: Building
        run: bazel build //...
      - name: Testing
        run: bazel test //...
